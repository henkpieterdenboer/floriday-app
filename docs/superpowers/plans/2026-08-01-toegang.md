# Toegang — implementatieplan (fase 1 van deelproject B)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Inloggen met e-mailadres en wachtwoord, gebruikersbeheer door een beheerder met
uitnodiging per e-mail, en Microsoft Entra voorbereid maar uitgeschakeld tot het ingericht is.

**Architecture:** NextAuth met JWT-sessies naast de bestaande synchronisatie, in dezelfde
applicatie en database. De beslisregels rond Entra zitten in pure functies zodat ze
uitputtend te testen zijn zonder een identity provider.

**Tech Stack:** NextAuth · @node-rs/argon2 · Nodemailer · Prisma 6 · Zod · Vitest

**Spec:** `docs/superpowers/specs/2026-08-01-toegang-en-zoekinterface-design.md`

**Fase 2** (het zoekscherm) krijgt een eigen plan zodra dit werkt.

---

## Wat de vorige bouwronde heeft geleerd

Deelproject A leverde negentien taken op waarvan er tien een fout in mijn plan bevatten.
Elke keer gevonden doordat een subagent weigerde een tegenstrijdigheid glad te strijken, of
doordat er echt gedraaid en gemeten werd in plaats van geredeneerd. De code in dit plan is
opnieuw niet gedraaid. Behandel elk codeblok als een voorstel, niet als waarheid.

Waar dat hier het meest waarschijnlijk speelt:

- **NextAuth-versies verschillen sterk.** v4 en v5 hebben een andere configuratievorm,
  andere imports en een andere middleware. Controleer wat er geïnstalleerd wordt voordat je
  mijn code overneemt.
- **Native modules op Windows.** argon2 heeft varianten met en zonder buildstap.
- **Next.js 16 met React 19** wijkt op punten af van wat in documentatie en voorbeelden staat.

## Over de schermtaken

De taken 1 tot en met 8 bevatten volledige code; dat is logica waar een verkeerde aanname
stille schade doet. De schermtaken (9 tot en met 11) zijn bewust op gedragsniveau
beschreven in plaats van uitgeschreven als React-componenten.

De reden: bij die schermen zit het risico niet in de opmaak maar in het gedrag — een
inlogscherm dat verraadt welke adressen bestaan, een doorverwijzing die naar buiten wijst,
een server action die alleen op de middleware vertrouwt. Die eisen staan er scherp genoeg
in dat er geen interpretatieruimte is. Hoe de knop eruitziet mag de uitvoerder bepalen; wat
er wel en niet gebeurt niet.

---

## Bestandsstructuur

```
prisma/schema.prisma                      User, Invitation, UserRole erbij

src/lib/mail.ts                           Nodemailer: Ethereal in ontwikkeling, Resend in productie

src/features/auth/
  password.ts                             wachtwoord hashen en verifiëren
  invitation-token.ts                     token genereren en hashen — puur
  entra-linking.ts                        mag deze SSO-aanmelding door — puur
  invitations.ts                          uitnodigingen aanmaken en inwisselen
  users.ts                                gebruikers opzoeken, aanmaken, deactiveren
  auth-config.ts                          NextAuth-configuratie
  emails/invitation.ts                    inhoud van de uitnodigingsmail

src/app/api/auth/[...nextauth]/route.ts   NextAuth route
src/middleware.ts                         weigert zonder geldige sessie

src/app/(public)/login/page.tsx
src/app/(public)/uitnodiging/[token]/page.tsx
src/app/(protected)/layout.tsx
src/app/(protected)/beheer/gebruikers/page.tsx

scripts/create-admin.ts                   eerste beheerder aanmaken
```

---

## Taak 1: Datamodel

**Files:**
- Modify: `prisma/schema.prisma`

- [ ] **Stap 1: Voeg de modellen toe**

Onderaan `prisma/schema.prisma`:

```prisma
enum UserRole {
  ADMIN
  VIEWER
}

model User {
  id           String    @id @default(uuid()) @db.Uuid
  email        String    @unique
  name         String
  /// Blijft leeg voor wie zich uitsluitend via Entra aanmeldt. Zegt dus niets over
  /// of iemand toegang heeft - dat doen isActive en het bestaan van het account.
  passwordHash String?
  role         UserRole  @default(VIEWER)
  isActive     Boolean   @default(true)
  lastLoginAt  DateTime? @db.Timestamptz
  createdAt    DateTime  @default(now()) @db.Timestamptz

  invitations  Invitation[]

  @@index([email])
}

model Invitation {
  id        String    @id @default(uuid()) @db.Uuid
  userId    String    @db.Uuid
  /// Alleen de hash. Wie de database inziet mag geen uitnodiging kunnen inwisselen.
  tokenHash String    @unique
  expiresAt DateTime  @db.Timestamptz
  usedAt    DateTime? @db.Timestamptz
  createdAt DateTime  @default(now()) @db.Timestamptz

  user      User      @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
}
```

- [ ] **Stap 2: Bekijk de DDL voordat je hem toepast**

Run: `npm run db:push:dry`
Verwacht: `CREATE TYPE "UserRole"`, `CREATE TABLE "User"`, `CREATE TABLE "Invitation"` en
een foreign key. Géén `DROP` of `ALTER` op bestaande tabellen — die bevatten 525.458
aanbodregels. Zie je iets destructiefs, stop dan en meld het.

- [ ] **Stap 3: Pas toe**

```bash
npm run db:push
npx prisma generate
```

- [ ] **Stap 4: Controleer onafhankelijk**

```bash
node -e "import('dotenv/config').then(async()=>{const {Pool}=await import('@neondatabase/serverless');const p=new Pool({connectionString:process.env.DIRECT_URL});console.log((await p.query(\"select table_name from information_schema.tables where table_schema='public' order by 1\")).rows.map(r=>r.table_name).join(', '));await p.end()})"
```

Verwacht: de zes bestaande tabellen plus `Invitation` en `User`.

- [ ] **Stap 5: Commit**

```bash
git add prisma/schema.prisma prisma/applied.prisma
git commit -m "feat: add user and invitation models"
```

---

## Taak 2: Wachtwoorden hashen

**Files:**
- Create: `src/features/auth/password.ts`
- Test: `tests/unit/auth/password.test.ts`

- [ ] **Stap 1: Installeer argon2**

```bash
npm install @node-rs/argon2
```

`@node-rs/argon2` is gekozen boven `argon2` omdat het voorgebouwde binaries levert en dus
geen buildstap met Visual Studio nodig heeft op Windows. Werkt de installatie niet, meld
dat dan in plaats van over te stappen op bcrypt zonder overleg.

- [ ] **Stap 2: Schrijf de falende test**

`tests/unit/auth/password.test.ts`:

```typescript
import { describe, expect, it } from "vitest";
import { hashPassword, verifyPassword } from "@/features/auth/password";

describe("password hashing", () => {
  it("accepts the correct password", async () => {
    const hash = await hashPassword("correct horse battery staple");
    expect(await verifyPassword(hash, "correct horse battery staple")).toBe(true);
  });

  it("rejects the wrong password", async () => {
    const hash = await hashPassword("correct horse battery staple");
    expect(await verifyPassword(hash, "wrong")).toBe(false);
  });

  it("produces a different hash every time for the same password", async () => {
    const a = await hashPassword("same");
    const b = await hashPassword("same");
    expect(a).not.toBe(b);
  });

  it("returns false instead of throwing on a malformed hash", async () => {
    expect(await verifyPassword("not-a-hash", "anything")).toBe(false);
  });

  it("handles a password with unicode and spaces", async () => {
    const password = "Wachtwoord met ëéï en spaties 🌷";
    const hash = await hashPassword(password);
    expect(await verifyPassword(hash, password)).toBe(true);
  });
});
```

- [ ] **Stap 3: Draai de test om te zien dat hij faalt**

Run: `npm run test:unit -- tests/unit/auth/password.test.ts`
Verwacht: FAIL, module niet gevonden.

- [ ] **Stap 4: Schrijf de implementatie**

`src/features/auth/password.ts`:

```typescript
import { hash, verify } from "@node-rs/argon2";

/**
 * OWASP-aanbeveling voor argon2id op het moment van schrijven. Bewust hier vastgelegd in
 * plaats van op de standaardwaarden vertrouwen, zodat een wijziging zichtbaar is in de
 * versiegeschiedenis.
 */
const OPTIONS = {
  memoryCost: 19456,
  timeCost: 2,
  parallelism: 1,
} as const;

export async function hashPassword(password: string): Promise<string> {
  return hash(password, OPTIONS);
}

/**
 * Geeft false terug in plaats van te gooien wanneer de opgeslagen hash onbruikbaar is.
 * Een kapotte hash in de database mag een inlogpoging laten mislukken, niet de hele
 * aanmeldroute laten crashen.
 */
export async function verifyPassword(storedHash: string, password: string): Promise<boolean> {
  try {
    return await verify(storedHash, password);
  } catch {
    return false;
  }
}
```

- [ ] **Stap 5: Draai de test opnieuw**

Run: `npm run test:unit -- tests/unit/auth/password.test.ts`
Verwacht: PASS, 5 tests.

Duurt dit merkbaar lang, dan is dat goed nieuws: argon2 hoort traag te zijn. Duurt één hash
meer dan een seconde, meld dat dan — dan staat `memoryCost` te hoog voor deze machine.

- [ ] **Stap 6: Commit**

```bash
git add src/features/auth/password.ts tests/unit/auth/password.test.ts package.json package-lock.json
git commit -m "feat: hash passwords with argon2id"
```

---

## Taak 3: Uitnodigingstokens

**Files:**
- Create: `src/features/auth/invitation-token.ts`
- Test: `tests/unit/auth/invitation-token.test.ts`

- [ ] **Stap 1: Schrijf de falende test**

`tests/unit/auth/invitation-token.test.ts`:

```typescript
import { describe, expect, it } from "vitest";
import {
  createInvitationToken,
  hashInvitationToken,
  isExpired,
} from "@/features/auth/invitation-token";

describe("createInvitationToken", () => {
  it("produces a token and its hash", () => {
    const { token, tokenHash } = createInvitationToken();
    expect(token.length).toBeGreaterThan(30);
    expect(tokenHash).toBe(hashInvitationToken(token));
  });

  it("never produces the same token twice", () => {
    const tokens = new Set(Array.from({ length: 200 }, () => createInvitationToken().token));
    expect(tokens.size).toBe(200);
  });

  it("produces a url-safe token", () => {
    for (let i = 0; i < 50; i++) {
      expect(createInvitationToken().token).toMatch(/^[A-Za-z0-9_-]+$/);
    }
  });
});

describe("hashInvitationToken", () => {
  it("is stable for the same input", () => {
    expect(hashInvitationToken("abc")).toBe(hashInvitationToken("abc"));
  });

  it("differs for different input", () => {
    expect(hashInvitationToken("abc")).not.toBe(hashInvitationToken("abd"));
  });

  it("does not contain the token itself", () => {
    expect(hashInvitationToken("geheim")).not.toContain("geheim");
  });
});

describe("isExpired", () => {
  const now = new Date("2026-08-01T12:00:00.000Z");

  it("is false before the expiry moment", () => {
    expect(isExpired(new Date("2026-08-01T12:00:01.000Z"), now)).toBe(false);
  });

  it("is true after the expiry moment", () => {
    expect(isExpired(new Date("2026-08-01T11:59:59.000Z"), now)).toBe(true);
  });

  it("is true exactly at the expiry moment", () => {
    expect(isExpired(now, now)).toBe(true);
  });
});
```

- [ ] **Stap 2: Draai de test om te zien dat hij faalt**

Run: `npm run test:unit -- tests/unit/auth/invitation-token.test.ts`
Verwacht: FAIL, module niet gevonden.

- [ ] **Stap 3: Schrijf de implementatie**

`src/features/auth/invitation-token.ts`:

```typescript
import { createHash, randomBytes } from "node:crypto";

/** Zeven dagen, in milliseconden. */
export const INVITATION_LIFETIME_MS = 7 * 24 * 60 * 60 * 1000;

export interface NewInvitationToken {
  /** Gaat naar de gebruiker, in de mail. Wordt nergens opgeslagen. */
  token: string;
  /** Gaat de database in. */
  tokenHash: string;
}

export function createInvitationToken(): NewInvitationToken {
  const token = randomBytes(32).toString("base64url");
  return { token, tokenHash: hashInvitationToken(token) };
}

/**
 * SHA-256 volstaat hier, anders dan bij wachtwoorden. Een token van 32 willekeurige bytes
 * is niet te raden of te bruteforcen, dus het langzame hashen dat wachtwoorden nodig
 * hebben voegt niets toe - het maakt het opzoeken alleen traag.
 */
export function hashInvitationToken(token: string): string {
  return createHash("sha256").update(token).digest("hex");
}

/** Op het verloopmoment zelf is de uitnodiging al verlopen. */
export function isExpired(expiresAt: Date, now: Date): boolean {
  return expiresAt.getTime() <= now.getTime();
}
```

- [ ] **Stap 4: Draai de test opnieuw**

Run: `npm run test:unit -- tests/unit/auth/invitation-token.test.ts`
Verwacht: PASS, 9 tests.

- [ ] **Stap 5: Commit**

```bash
git add src/features/auth/invitation-token.ts tests/unit/auth/invitation-token.test.ts
git commit -m "feat: generate and hash invitation tokens"
```

---

## Taak 4: De Entra-koppelregels

Dit is de kern van de toegangsbeveiliging. Alles staat in één pure functie zodat elke regel
uitputtend te testen is zonder identity provider.

**Files:**
- Create: `src/features/auth/entra-linking.ts`
- Test: `tests/unit/auth/entra-linking.test.ts`

- [ ] **Stap 1: Schrijf de falende test**

`tests/unit/auth/entra-linking.test.ts`:

```typescript
import { describe, expect, it } from "vitest";
import { decideEntraSignIn } from "@/features/auth/entra-linking";

const account = {
  id: "u1",
  email: "henk@coloriginz.com",
  isActive: true,
  passwordHash: null as string | null,
};

describe("decideEntraSignIn", () => {
  it("allows an existing active account", () => {
    const result = decideEntraSignIn({
      profileEmail: "henk@coloriginz.com",
      profileEmailVerified: true,
      account,
    });
    expect(result).toEqual({ allowed: true, userId: "u1" });
  });

  it("allows an account that has no password yet", () => {
    const result = decideEntraSignIn({
      profileEmail: "henk@coloriginz.com",
      profileEmailVerified: true,
      account: { ...account, passwordHash: null },
    });
    expect(result.allowed).toBe(true);
  });

  it("allows an account that does have a password", () => {
    const result = decideEntraSignIn({
      profileEmail: "henk@coloriginz.com",
      profileEmailVerified: true,
      account: { ...account, passwordHash: "$argon2id$..." },
    });
    expect(result.allowed).toBe(true);
  });

  it("refuses when no account exists, and does not create one", () => {
    const result = decideEntraSignIn({
      profileEmail: "vreemde@elders.com",
      profileEmailVerified: true,
      account: null,
    });
    expect(result).toEqual({ allowed: false, reason: "no-account" });
  });

  it("refuses a deactivated account", () => {
    const result = decideEntraSignIn({
      profileEmail: "henk@coloriginz.com",
      profileEmailVerified: true,
      account: { ...account, isActive: false },
    });
    expect(result).toEqual({ allowed: false, reason: "deactivated" });
  });

  it("refuses when the provider did not verify the address", () => {
    const result = decideEntraSignIn({
      profileEmail: "henk@coloriginz.com",
      profileEmailVerified: false,
      account,
    });
    expect(result).toEqual({ allowed: false, reason: "email-not-verified" });
  });

  it("refuses when the provider sends no address at all", () => {
    const result = decideEntraSignIn({
      profileEmail: null,
      profileEmailVerified: true,
      account,
    });
    expect(result).toEqual({ allowed: false, reason: "no-email" });
  });

  it("matches case insensitively", () => {
    const result = decideEntraSignIn({
      profileEmail: "Henk@Coloriginz.COM",
      profileEmailVerified: true,
      account,
    });
    expect(result.allowed).toBe(true);
  });

  it("ignores surrounding whitespace", () => {
    const result = decideEntraSignIn({
      profileEmail: "  henk@coloriginz.com  ",
      profileEmailVerified: true,
      account,
    });
    expect(result.allowed).toBe(true);
  });

  it("refuses when the addresses differ, even by one character", () => {
    const result = decideEntraSignIn({
      profileEmail: "henk@coloriginz.co",
      profileEmailVerified: true,
      account,
    });
    expect(result).toEqual({ allowed: false, reason: "email-mismatch" });
  });
});
```

- [ ] **Stap 2: Draai de test om te zien dat hij faalt**

Run: `npm run test:unit -- tests/unit/auth/entra-linking.test.ts`
Verwacht: FAIL, module niet gevonden.

- [ ] **Stap 3: Schrijf de implementatie**

`src/features/auth/entra-linking.ts`:

```typescript
export interface LinkableAccount {
  id: string;
  email: string;
  isActive: boolean;
  passwordHash: string | null;
}

export interface EntraSignInInput {
  /** Wat de provider aanlevert. Kan ontbreken. */
  profileEmail: string | null;
  /** Of de provider het adres als geverifieerd doorgeeft. */
  profileEmailVerified: boolean;
  /** Het account dat op dat adres gevonden is, of null. */
  account: LinkableAccount | null;
}

export type EntraSignInDecision =
  | { allowed: true; userId: string }
  | {
      allowed: false;
      reason: "no-email" | "email-not-verified" | "no-account" | "deactivated" | "email-mismatch";
    };

export function normaliseEmail(email: string): string {
  return email.trim().toLowerCase();
}

/**
 * Bepaalt of een aanmelding via Entra door mag. Twee regels dragen de beveiliging:
 *
 * 1. Er wordt nooit een account aangemaakt. SSO geeft toegang tot bestaande accounts, meer
 *    niet. Zonder die regel kan iedereen met een werkmailadres binnenlopen zodra de
 *    koppeling live gaat.
 * 2. Het adres moet geverifieerd zijn en exact overeenkomen. Zonder de eerste eis vertrouwen
 *    we een provider op zijn woord; zonder de tweede kan iemand met een ander account op een
 *    bestaand account belanden.
 *
 * Of het account al een wachtwoord heeft doet niet ter zake: wie is uitgenodigd maar nog
 * nooit heeft ingelogd, neemt zijn account hiermee in gebruik zonder ooit een wachtwoord te
 * kiezen.
 */
export function decideEntraSignIn(input: EntraSignInInput): EntraSignInDecision {
  const { profileEmail, profileEmailVerified, account } = input;

  if (!profileEmail || normaliseEmail(profileEmail) === "") {
    return { allowed: false, reason: "no-email" };
  }
  if (!profileEmailVerified) {
    return { allowed: false, reason: "email-not-verified" };
  }
  if (!account) {
    return { allowed: false, reason: "no-account" };
  }
  if (normaliseEmail(profileEmail) !== normaliseEmail(account.email)) {
    return { allowed: false, reason: "email-mismatch" };
  }
  if (!account.isActive) {
    return { allowed: false, reason: "deactivated" };
  }

  return { allowed: true, userId: account.id };
}
```

Let op de vololgorde van de controles: het adres wordt vergeleken vóórdat er op
gedeactiveerd wordt gecontroleerd. Dat is bewust — bij een niet-overeenkomend adres hoort de
uitkomst niet af te hangen van de toestand van een account dat er toch niet bij hoort.

- [ ] **Stap 4: Draai de test opnieuw**

Run: `npm run test:unit -- tests/unit/auth/entra-linking.test.ts`
Verwacht: PASS, 10 tests.

- [ ] **Stap 5: Commit**

```bash
git add src/features/auth/entra-linking.ts tests/unit/auth/entra-linking.test.ts
git commit -m "feat: decide entra sign-in without ever creating an account"
```

---

## Taak 5: Gebruikers en uitnodigingen in de database

**Files:**
- Create: `src/features/auth/users.ts`
- Create: `src/features/auth/invitations.ts`
- Test: `tests/integration/auth/invitations.test.ts`

- [ ] **Stap 1: Schrijf de falende test**

`tests/integration/auth/invitations.test.ts`:

```typescript
import "dotenv/config";
import { afterAll, beforeEach, describe, expect, it } from "vitest";
import { prisma } from "@/lib/db";
import { createUser, findUserByEmail, setUserActive } from "@/features/auth/users";
import { createInvitation, redeemInvitation } from "@/features/auth/invitations";

const EMAIL = "test-invitation@example.test";

async function cleanup(): Promise<void> {
  await prisma.user.deleteMany({ where: { email: EMAIL } });
}

beforeEach(cleanup);
afterAll(async () => {
  await cleanup();
  await prisma.$disconnect();
});

describe("users", () => {
  it("creates a user without a password", async () => {
    const user = await createUser({ email: EMAIL, name: "Test", role: "VIEWER" });
    expect(user.passwordHash).toBeNull();
    expect(user.isActive).toBe(true);
  });

  it("finds a user case insensitively", async () => {
    await createUser({ email: EMAIL, name: "Test", role: "VIEWER" });
    expect(await findUserByEmail(EMAIL.toUpperCase())).not.toBeNull();
  });

  it("stores the email in lower case", async () => {
    const user = await createUser({ email: EMAIL.toUpperCase(), name: "Test", role: "VIEWER" });
    expect(user.email).toBe(EMAIL);
  });

  it("refuses a duplicate email", async () => {
    await createUser({ email: EMAIL, name: "Test", role: "VIEWER" });
    await expect(createUser({ email: EMAIL, name: "Nog een", role: "VIEWER" })).rejects.toThrow();
  });
});

describe("invitations", () => {
  it("redeems a valid invitation and sets a password", async () => {
    const user = await createUser({ email: EMAIL, name: "Test", role: "VIEWER" });
    const { token } = await createInvitation(user.id);

    const result = await redeemInvitation(token, "een goed wachtwoord");
    expect(result.ok).toBe(true);

    const updated = await findUserByEmail(EMAIL);
    expect(updated?.passwordHash).not.toBeNull();
  });

  it("refuses the same token twice", async () => {
    const user = await createUser({ email: EMAIL, name: "Test", role: "VIEWER" });
    const { token } = await createInvitation(user.id);

    await redeemInvitation(token, "eerste wachtwoord");
    const second = await redeemInvitation(token, "tweede wachtwoord");
    expect(second).toEqual({ ok: false, reason: "already-used" });
  });

  it("refuses an expired invitation", async () => {
    const user = await createUser({ email: EMAIL, name: "Test", role: "VIEWER" });
    const { token, id } = await createInvitation(user.id);
    await prisma.invitation.update({
      where: { id },
      data: { expiresAt: new Date(Date.now() - 1000) },
    });

    expect(await redeemInvitation(token, "wachtwoord")).toEqual({ ok: false, reason: "expired" });
  });

  it("refuses an unknown token", async () => {
    expect(await redeemInvitation("bestaat-niet", "wachtwoord"))
      .toEqual({ ok: false, reason: "not-found" });
  });

  it("refuses an invitation for a deactivated user", async () => {
    const user = await createUser({ email: EMAIL, name: "Test", role: "VIEWER" });
    const { token } = await createInvitation(user.id);
    await setUserActive(user.id, false);

    expect(await redeemInvitation(token, "wachtwoord"))
      .toEqual({ ok: false, reason: "deactivated" });
  });

  it("never stores the token itself", async () => {
    const user = await createUser({ email: EMAIL, name: "Test", role: "VIEWER" });
    const { token } = await createInvitation(user.id);

    const stored = await prisma.invitation.findMany({ where: { userId: user.id } });
    expect(stored[0].tokenHash).not.toBe(token);
    expect(stored[0].tokenHash).not.toContain(token);
  });
});
```

- [ ] **Stap 2: Draai de test om te zien dat hij faalt**

Run: `npm test -- tests/integration/auth/invitations.test.ts`
Verwacht: FAIL, modules niet gevonden.

- [ ] **Stap 3: Schrijf `users.ts`**

```typescript
import type { User, UserRole } from "@prisma/client";
import { prisma } from "@/lib/db";
import { normaliseEmail } from "@/features/auth/entra-linking";

export interface CreateUserInput {
  email: string;
  name: string;
  role: UserRole;
}

/** Adressen worden genormaliseerd opgeslagen, zodat opzoeken altijd werkt. */
export async function createUser(input: CreateUserInput): Promise<User> {
  return prisma.user.create({
    data: {
      email: normaliseEmail(input.email),
      name: input.name,
      role: input.role,
    },
  });
}

export async function findUserByEmail(email: string): Promise<User | null> {
  return prisma.user.findUnique({ where: { email: normaliseEmail(email) } });
}

export async function setUserActive(userId: string, isActive: boolean): Promise<void> {
  await prisma.user.update({ where: { id: userId }, data: { isActive } });
}

export async function recordLogin(userId: string): Promise<void> {
  await prisma.user.update({ where: { id: userId }, data: { lastLoginAt: new Date() } });
}

export async function listUsers(): Promise<User[]> {
  return prisma.user.findMany({ orderBy: [{ isActive: "desc" }, { name: "asc" }] });
}
```

- [ ] **Stap 4: Schrijf `invitations.ts`**

```typescript
import { prisma } from "@/lib/db";
import { hashPassword } from "@/features/auth/password";
import {
  INVITATION_LIFETIME_MS,
  createInvitationToken,
  hashInvitationToken,
  isExpired,
} from "@/features/auth/invitation-token";

export interface CreatedInvitation {
  id: string;
  /** Alleen hier beschikbaar. Gaat naar de mail en wordt nergens opgeslagen. */
  token: string;
  expiresAt: Date;
}

export async function createInvitation(userId: string): Promise<CreatedInvitation> {
  const { token, tokenHash } = createInvitationToken();
  const expiresAt = new Date(Date.now() + INVITATION_LIFETIME_MS);

  const invitation = await prisma.invitation.create({
    data: { userId, tokenHash, expiresAt },
  });

  return { id: invitation.id, token, expiresAt };
}

export type RedeemResult =
  | { ok: true; userId: string }
  | { ok: false; reason: "not-found" | "already-used" | "expired" | "deactivated" };

/**
 * Wisselt een uitnodiging in voor een wachtwoord. De volgorde van controles is bewust:
 * eerst bestaan, dan gebruikt, dan verlopen, dan de toestand van het account. Zo krijgt de
 * gebruiker de meest specifieke reden terug.
 */
export async function redeemInvitation(
  token: string,
  password: string,
): Promise<RedeemResult> {
  const invitation = await prisma.invitation.findUnique({
    where: { tokenHash: hashInvitationToken(token) },
    include: { user: true },
  });

  if (!invitation) return { ok: false, reason: "not-found" };
  if (invitation.usedAt) return { ok: false, reason: "already-used" };
  if (isExpired(invitation.expiresAt, new Date())) return { ok: false, reason: "expired" };
  if (!invitation.user.isActive) return { ok: false, reason: "deactivated" };

  const passwordHash = await hashPassword(password);

  await prisma.$transaction([
    prisma.user.update({ where: { id: invitation.userId }, data: { passwordHash } }),
    prisma.invitation.update({ where: { id: invitation.id }, data: { usedAt: new Date() } }),
  ]);

  return { ok: true, userId: invitation.userId };
}
```

- [ ] **Stap 5: Draai de test opnieuw**

Run: `npm test -- tests/integration/auth/invitations.test.ts`
Verwacht: PASS, 11 tests.

- [ ] **Stap 6: Commit**

```bash
git add src/features/auth/users.ts src/features/auth/invitations.ts tests/integration/auth
git commit -m "feat: manage users and redeemable invitations"
```

---

## Taak 6: E-mail versturen

**Files:**
- Create: `src/lib/mail.ts`
- Create: `src/features/auth/emails/invitation.ts`
- Modify: `src/lib/env.ts`, `.env.example`

- [ ] **Stap 1: Installeer Nodemailer**

```bash
npm install nodemailer
npm install -D @types/nodemailer
```

- [ ] **Stap 2: Breid de omgevingsconfiguratie uit**

In `src/lib/env.ts`, voeg toe aan `envSchema`:

```typescript
  APP_URL: z.string().url(),

  // E-mail. Ontbreken deze, dan valt de applicatie terug op Ethereal en wordt er niets
  // echt verstuurd - dat is wat je wilt tijdens ontwikkelen.
  SMTP_HOST: z.string().optional(),
  SMTP_PORT: z.coerce.number().int().optional(),
  SMTP_USER: z.string().optional(),
  SMTP_PASSWORD: z.string().optional(),
  MAIL_FROM: z.string().optional(),

  // Entra. Ontbreken deze, dan verschijnt de aanmeldknop niet.
  AZURE_AD_CLIENT_ID: z.string().optional(),
  AZURE_AD_CLIENT_SECRET: z.string().optional(),
  AZURE_AD_TENANT_ID: z.string().optional(),

  NEXTAUTH_SECRET: z.string().min(1),
```

In `.env.example` dezelfde sleutels, leeg, met een korte toelichting per blok. Vul in `.env`
alvast `APP_URL=http://localhost:3000` en genereer een `NEXTAUTH_SECRET`:

```bash
node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
```

- [ ] **Stap 3: Schrijf `src/lib/mail.ts`**

```typescript
import nodemailer, { type Transporter } from "nodemailer";
import { getEnv } from "@/lib/env";

export interface Mail {
  to: string;
  subject: string;
  text: string;
  html: string;
}

let cached: Transporter | null = null;

/**
 * Zonder SMTP-instellingen wordt een Ethereal-testaccount gebruikt: berichten komen dan
 * nergens echt aan, maar zijn wel via een link te bekijken. Dat is tijdens ontwikkelen
 * precies goed, en het voorkomt dat een halfaf systeem echte mail naar collega's stuurt.
 */
async function getTransporter(): Promise<Transporter> {
  if (cached) return cached;

  const env = getEnv();

  if (env.SMTP_HOST && env.SMTP_PORT && env.SMTP_USER && env.SMTP_PASSWORD) {
    cached = nodemailer.createTransport({
      host: env.SMTP_HOST,
      port: env.SMTP_PORT,
      secure: env.SMTP_PORT === 465,
      auth: { user: env.SMTP_USER, pass: env.SMTP_PASSWORD },
    });
    return cached;
  }

  const testAccount = await nodemailer.createTestAccount();
  cached = nodemailer.createTransport({
    host: "smtp.ethereal.email",
    port: 587,
    secure: false,
    auth: { user: testAccount.user, pass: testAccount.pass },
  });
  return cached;
}

/** Geeft de preview-URL terug wanneer er via Ethereal is verstuurd, anders null. */
export async function sendMail(mail: Mail): Promise<string | null> {
  const transporter = await getTransporter();
  const env = getEnv();

  const info = await transporter.sendMail({
    from: env.MAIL_FROM ?? "Floriday middleware <noreply@coloriginz.com>",
    to: mail.to,
    subject: mail.subject,
    text: mail.text,
    html: mail.html,
  });

  const preview = nodemailer.getTestMessageUrl(info);
  return typeof preview === "string" ? preview : null;
}
```

- [ ] **Stap 4: Schrijf de uitnodigingsmail**

`src/features/auth/emails/invitation.ts`:

```typescript
import type { Mail } from "@/lib/mail";

export interface InvitationMailInput {
  to: string;
  name: string;
  invitationUrl: string;
  expiresAt: Date;
  /** Toon de Entra-mogelijkheid alleen als die daadwerkelijk aanstaat. */
  entraEnabled: boolean;
}

function formatDate(date: Date): string {
  return new Intl.DateTimeFormat("nl-NL", { dateStyle: "long" }).format(date);
}

export function buildInvitationMail(input: InvitationMailInput): Mail {
  const { to, name, invitationUrl, expiresAt, entraEnabled } = input;

  const entraLine = entraEnabled
    ? "Je kunt ook meteen aanmelden met je werkaccount van Microsoft; dan heb je geen wachtwoord nodig."
    : "";

  const text = [
    `Hallo ${name},`,
    "",
    "Je hebt toegang gekregen tot het aanbodoverzicht van Coloriginz.",
    "",
    `Stel hier je wachtwoord in: ${invitationUrl}`,
    `Deze link werkt tot ${formatDate(expiresAt)}.`,
    entraLine,
  ].filter(Boolean).join("\n");

  const html = `
    <p>Hallo ${name},</p>
    <p>Je hebt toegang gekregen tot het aanbodoverzicht van Coloriginz.</p>
    <p><a href="${invitationUrl}" style="background-color:#0f7b3f;color:#ffffff;padding:12px 20px;border-radius:6px;text-decoration:none;display:inline-block">Wachtwoord instellen</a></p>
    <p>Deze link werkt tot ${formatDate(expiresAt)}.</p>
    ${entraLine ? `<p>${entraLine}</p>` : ""}
  `.trim();

  return { to, subject: "Toegang tot het aanbodoverzicht", text, html };
}
```

- [ ] **Stap 5: Schrijf een test voor de mailinhoud**

`tests/unit/auth/invitation-mail.test.ts`:

```typescript
import { describe, expect, it } from "vitest";
import { buildInvitationMail } from "@/features/auth/emails/invitation";

const base = {
  to: "henk@coloriginz.com",
  name: "Henk Pieter",
  invitationUrl: "https://app.example/uitnodiging/abc",
  expiresAt: new Date("2026-08-08T12:00:00.000Z"),
};

describe("buildInvitationMail", () => {
  it("contains the link in both plain text and html", () => {
    const mail = buildInvitationMail({ ...base, entraEnabled: false });
    expect(mail.text).toContain(base.invitationUrl);
    expect(mail.html).toContain(base.invitationUrl);
  });

  it("mentions the expiry date", () => {
    const mail = buildInvitationMail({ ...base, entraEnabled: false });
    expect(mail.text).toContain("8 augustus 2026");
  });

  it("mentions the work account only when entra is enabled", () => {
    expect(buildInvitationMail({ ...base, entraEnabled: true }).text).toContain("werkaccount");
    expect(buildInvitationMail({ ...base, entraEnabled: false }).text).not.toContain("werkaccount");
  });

  it("addresses the recipient by name", () => {
    expect(buildInvitationMail({ ...base, entraEnabled: false }).text).toContain("Henk Pieter");
  });
});
```

- [ ] **Stap 6: Draai de tests**

Run: `npm run test:unit -- tests/unit/auth/invitation-mail.test.ts`
Verwacht: PASS, 4 tests.

Faalt de datumtest, controleer dan wat `Intl` op deze machine oplevert en pas de
verwachting aan naar de werkelijke uitvoer — niet de opmaak aanpassen om de test te laten
slagen.

- [ ] **Stap 7: Commit**

```bash
git add src/lib/mail.ts src/features/auth/emails src/lib/env.ts .env.example tests/unit/auth/invitation-mail.test.ts package.json package-lock.json
git commit -m "feat: send invitation emails, ethereal in development"
```

---

## Taak 7: NextAuth configureren

**Files:**
- Create: `src/features/auth/auth-config.ts`
- Create: `src/app/api/auth/[...nextauth]/route.ts`
- Create: `src/middleware.ts`

- [ ] **Stap 1: Installeer NextAuth en stel de versie vast**

```bash
npm install next-auth
npx npm list next-auth
```

**Meld welke versie geïnstalleerd is voordat je verder gaat.** De configuratievorm verschilt
wezenlijk tussen versie 4 en 5: v5 exporteert `handlers`, `auth`, `signIn` en `signOut` uit
één `NextAuth()`-aanroep, v4 gebruikt `NextAuthOptions` met een `[...nextauth]`-route. De
code hieronder gaat uit van **v5**. Is het v4 geworden, pas dan de vorm aan en beschrijf wat
je hebt gewijzigd; de beslisregels blijven identiek.

- [ ] **Stap 2: Schrijf de configuratie**

`src/features/auth/auth-config.ts`:

```typescript
import NextAuth, { type NextAuthConfig } from "next-auth";
import Credentials from "next-auth/providers/credentials";
import MicrosoftEntraID from "next-auth/providers/microsoft-entra-id";
import { getEnv } from "@/lib/env";
import { verifyPassword } from "@/features/auth/password";
import { decideEntraSignIn } from "@/features/auth/entra-linking";
import { findUserByEmail, recordLogin } from "@/features/auth/users";

const env = getEnv();

/** Entra doet alleen mee als het volledig geconfigureerd is. */
export const entraEnabled = Boolean(
  env.AZURE_AD_CLIENT_ID && env.AZURE_AD_CLIENT_SECRET && env.AZURE_AD_TENANT_ID,
);

const providers: NextAuthConfig["providers"] = [
  Credentials({
    credentials: {
      email: { label: "E-mailadres", type: "email" },
      password: { label: "Wachtwoord", type: "password" },
    },
    async authorize(credentials) {
      const email = typeof credentials?.email === "string" ? credentials.email : "";
      const password = typeof credentials?.password === "string" ? credentials.password : "";
      if (!email || !password) return null;

      const user = await findUserByEmail(email);
      // Geen account, geen wachtwoord ingesteld, of gedeactiveerd: alle drie leveren
      // hetzelfde antwoord op, zodat een inlogscherm niet verraadt welke adressen bestaan.
      if (!user || !user.passwordHash || !user.isActive) return null;
      if (!(await verifyPassword(user.passwordHash, password))) return null;

      await recordLogin(user.id);
      return { id: user.id, email: user.email, name: user.name, role: user.role };
    },
  }),
];

if (entraEnabled) {
  providers.push(
    MicrosoftEntraID({
      clientId: env.AZURE_AD_CLIENT_ID!,
      clientSecret: env.AZURE_AD_CLIENT_SECRET!,
      issuer: `https://login.microsoftonline.com/${env.AZURE_AD_TENANT_ID}/v2.0`,
    }),
  );
}

export const { handlers, auth, signIn, signOut } = NextAuth({
  providers,
  session: { strategy: "jwt" },
  pages: { signIn: "/login" },
  callbacks: {
    async signIn({ user, account, profile }) {
      if (account?.provider !== "microsoft-entra-id") return true;

      const profileEmail =
        (typeof profile?.email === "string" ? profile.email : null) ??
        (typeof user.email === "string" ? user.email : null);

      const existing = profileEmail ? await findUserByEmail(profileEmail) : null;

      const decision = decideEntraSignIn({
        profileEmail,
        // Entra levert dit als email_verified of als verified_primary_email. Controleer
        // tegen een echt profiel welke van de twee je krijgt.
        profileEmailVerified: profile?.email_verified === true,
        account: existing
          ? {
              id: existing.id,
              email: existing.email,
              isActive: existing.isActive,
              passwordHash: existing.passwordHash,
            }
          : null,
      });

      if (!decision.allowed) return `/login?fout=${decision.reason}`;

      await recordLogin(decision.userId);
      return true;
    },

    async jwt({ token, user }) {
      if (user) {
        token.userId = user.id;
        token.role = (user as { role?: string }).role ?? "VIEWER";
      }
      return token;
    },

    async session({ session, token }) {
      if (session.user) {
        session.user.id = String(token.userId ?? "");
        (session.user as { role?: string }).role = String(token.role ?? "VIEWER");
      }
      return session;
    },
  },
});
```

- [ ] **Stap 3: Schrijf de route**

`src/app/api/auth/[...nextauth]/route.ts`:

```typescript
export { GET, POST } from "@/features/auth/auth-config";
```

Bij v5 volstaat dit als `handlers` `{ GET, POST }` teruggeeft; controleer dat en pas zo
nodig aan naar `export const { GET, POST } = handlers;`.

- [ ] **Stap 4: Schrijf de middleware**

`src/middleware.ts`:

```typescript
import { auth } from "@/features/auth/auth-config";

export default auth((request) => {
  if (!request.auth) {
    const url = new URL("/login", request.nextUrl.origin);
    url.searchParams.set("verder", request.nextUrl.pathname);
    return Response.redirect(url);
  }
});

/**
 * De cron-routes staan er bewust buiten: die hebben hun eigen controle op CRON_SECRET en
 * worden door Vercel aangeroepen zonder sessie. Zou de middleware ze afvangen, dan stopt de
 * synchronisatie zonder dat iemand het merkt.
 */
export const config = {
  matcher: ["/aanbod/:path*", "/beheer/:path*"],
};
```

- [ ] **Stap 5: Controleer dat het bouwt**

```bash
npx tsc --noEmit
npm run build
```

Verwacht: beide zonder fouten, en de nieuwe route verschijnt in de build-uitvoer.

- [ ] **Stap 6: Commit**

```bash
git add src/features/auth/auth-config.ts src/app/api/auth src/middleware.ts package.json package-lock.json
git commit -m "feat: configure nextauth with credentials and optional entra"
```

---

## Taak 8: De eerste beheerder

**Files:**
- Create: `scripts/create-admin.ts`
- Modify: `package.json`

- [ ] **Stap 1: Schrijf het script**

```typescript
/**
 * Maakt de eerste beheerder aan. Zonder dit is er niemand die gebruikers kan uitnodigen,
 * en omdat er geen zelfregistratie is, kom je er anders nooit in.
 *
 * Gebruik: npm run create-admin -- --email jij@bedrijf.nl --naam "Jouw Naam"
 */
import "dotenv/config";
import { prisma } from "@/lib/db";
import { createUser, findUserByEmail } from "@/features/auth/users";
import { createInvitation } from "@/features/auth/invitations";
import { buildInvitationMail } from "@/features/auth/emails/invitation";
import { sendMail } from "@/lib/mail";
import { getEnv } from "@/lib/env";

function readFlag(name: string): string | undefined {
  const index = process.argv.indexOf(`--${name}`);
  return index === -1 ? undefined : process.argv[index + 1];
}

async function main(): Promise<void> {
  const email = readFlag("email");
  const name = readFlag("naam");

  if (!email || !name) {
    console.error('Gebruik: npm run create-admin -- --email jij@bedrijf.nl --naam "Jouw Naam"');
    process.exit(1);
  }

  if (await findUserByEmail(email)) {
    console.error(`Er bestaat al een account voor ${email}.`);
    process.exit(1);
  }

  const user = await createUser({ email, name, role: "ADMIN" });
  const invitation = await createInvitation(user.id);
  const url = `${getEnv().APP_URL}/uitnodiging/${invitation.token}`;

  const preview = await sendMail(
    buildInvitationMail({
      to: user.email,
      name: user.name,
      invitationUrl: url,
      expiresAt: invitation.expiresAt,
      entraEnabled: false,
    }),
  );

  console.log(`Beheerder aangemaakt: ${user.email}`);
  console.log(`Uitnodiging: ${url}`);
  if (preview) console.log(`Mail bekijken: ${preview}`);

  await prisma.$disconnect();
}

main().catch(async (error: unknown) => {
  console.error(error);
  await prisma.$disconnect();
  process.exit(1);
});
```

De uitnodigings-URL wordt óók op de terminal getoond. Dat is bewust: bij de allereerste
beheerder is er nog geen werkende mailconfiguratie, en dan moet je er zonder mail in kunnen.

- [ ] **Stap 2: Voeg het script toe aan `package.json`**

```json
    "create-admin": "tsx scripts/create-admin.ts",
```

- [ ] **Stap 3: Draai het**

```bash
npm run create-admin -- --email henkpieterdenboer@gmail.com --naam "Henk Pieter den Boer"
```

Verwacht: een aangemaakte beheerder, een uitnodigings-URL, en een Ethereal-preview-link.
Open die link en controleer dat de mail er goed uitziet.

- [ ] **Stap 4: Commit**

```bash
git add scripts/create-admin.ts package.json
git commit -m "feat: add script to create the first administrator"
```

---

## Taak 9: Het inlogscherm

**Files:**
- Create: `src/app/(public)/login/page.tsx`
- Create: `src/app/(public)/login/login-form.tsx`

- [ ] **Stap 1: Installeer shadcn/ui**

```bash
npx shadcn@latest init
npx shadcn@latest add button input label card alert
```

Kies bij de vragen de standaardopties die bij Tailwind en de App Router passen. Meld welke
keuzes je hebt gemaakt.

- [ ] **Stap 2: Schrijf het formulier**

`src/app/(public)/login/login-form.tsx` — een client component met een e-mail- en
wachtwoordveld dat `signIn("credentials", ...)` aanroept, en daaronder, **alleen als
`entraEnabled` waar is**, een knop die `signIn("microsoft-entra-id")` aanroept.

Eisen aan het gedrag:

- Een mislukte aanmelding toont één algemene melding: "E-mailadres of wachtwoord klopt
  niet." Nooit onderscheid maken tussen een onbekend adres en een verkeerd wachtwoord —
  anders is het inlogscherm een middel om te achterhalen wie er een account heeft.
- De foutcodes uit de Entra-afwijzing (`no-account`, `deactivated`, `email-mismatch`,
  `email-not-verified`, `no-email`) komen binnen als `?fout=` in de URL. Vertaal die naar
  begrijpelijke Nederlandse zinnen. Bij `no-account`: "Dit account is bij ons niet bekend.
  Vraag een beheerder om je toe te voegen."
- Na een geslaagde aanmelding doorsturen naar de waarde van `?verder=` als die er is, anders
  naar `/aanbod`. Controleer dat die waarde een pad binnen de applicatie is en geen volledige
  URL — een open doorverwijzing is een klassieke fout op precies deze plek.

- [ ] **Stap 3: Schrijf de pagina**

`src/app/(public)/login/page.tsx` — server component die `entraEnabled` doorgeeft aan het
formulier en het geheel centreert.

- [ ] **Stap 4: Controleer met de hand**

Start de server (`npm run dev`) en controleer in een browser:

- Aanmelden met een fout wachtwoord toont de algemene melding.
- Aanmelden met de beheerder werkt nadat je in taak 10 een wachtwoord hebt ingesteld.
- Er is geen Entra-knop zichtbaar, want die variabelen staan niet in `.env`.
- `/aanbod` opvragen zonder sessie stuurt door naar `/login?verder=/aanbod`. Die pagina
  bestaat pas na taak 11; tot dan eindigt een geslaagde aanmelding op een 404 en dat is
  verwacht.
- Een `?verder=https://elders.example` in de URL stuurt **niet** naar buiten door.

- [ ] **Stap 5: Commit**

```bash
git add src/app/\(public\)/login components.json src/components package.json package-lock.json
git commit -m "feat: add login screen"
```

---

## Taak 10: Het uitnodigingsscherm

**Files:**
- Create: `src/app/(public)/uitnodiging/[token]/page.tsx`
- Create: `src/app/(public)/uitnodiging/[token]/set-password-form.tsx`
- Create: `src/features/auth/actions.ts`

- [ ] **Stap 1: Schrijf de server action**

`src/features/auth/actions.ts` met een `setPasswordAction` die het token en het wachtwoord
aanneemt, `redeemInvitation` aanroept en de uitkomst teruggeeft.

Eisen:

- Een wachtwoord van minder dan twaalf tekens wordt geweigerd, met een duidelijke melding.
  Valideer met Zod, zoals in de rest van het project.
- Het token komt uit de URL en gaat rechtstreeks naar `redeemInvitation`, dat op de hash
  zoekt. Log het token nooit.
- Elke afwijzingsreden krijgt een eigen Nederlandse zin: verlopen, al gebruikt, onbekend, of
  een gedeactiveerd account.

- [ ] **Stap 2: Schrijf het scherm**

Een formulier met wachtwoord en herhaling. Toont bij succes een bevestiging met een link
naar `/login`.

- [ ] **Stap 3: Controleer met de hand**

Gebruik de uitnodigings-URL uit taak 8:

- Een te kort wachtwoord wordt geweigerd.
- Een geldig wachtwoord werkt, en daarna kun je inloggen.
- Dezelfde link nogmaals openen meldt dat de uitnodiging al gebruikt is.

- [ ] **Stap 4: Commit**

```bash
git add src/app/\(public\)/uitnodiging src/features/auth/actions.ts
git commit -m "feat: let an invited user set a password"
```

---

## Taak 11: Het beheerscherm

**Files:**
- Create: `src/app/(protected)/layout.tsx`
- Create: `src/app/(protected)/beheer/gebruikers/page.tsx`
- Create: `src/app/(protected)/beheer/gebruikers/user-table.tsx`
- Create: `src/app/(protected)/beheer/gebruikers/invite-form.tsx`
- Modify: `src/features/auth/actions.ts`

- [ ] **Stap 1: Breid de server actions uit**

Voeg toe: een actie om een gebruiker uit te nodigen (aanmaken plus mail versturen), een om
een account te deactiveren of te heractiveren, en een om een nieuwe uitnodiging te sturen.

**Elke actie controleert eerst dat de aanroeper is aangemeld én de rol `ADMIN` heeft.** De
middleware beschermt de pagina, maar server actions zijn afzonderlijk aan te roepen — wie
alleen op de middleware vertrouwt, laat de deur aan de achterkant open.

- [ ] **Stap 2: Schrijf de beschermde layout**

`src/app/(protected)/layout.tsx` haalt de sessie op, stuurt door naar `/login` als die
ontbreekt, en toont een eenvoudige navigatie met de naam van de gebruiker, een link naar
`/aanbod`, een link naar `/beheer/gebruikers` (alleen voor beheerders) en afmelden.

- [ ] **Stap 2b: Maak een voorlopige `/aanbod`**

`src/app/(protected)/aanbod/page.tsx`:

```tsx
export default function AanbodPage() {
  return (
    <main className="p-8">
      <h1 className="text-xl font-semibold">Aanbod</h1>
      <p className="mt-2 text-sm opacity-70">
        Het zoekscherm wordt in fase 2 gebouwd.
      </p>
    </main>
  );
}
```

Deze pagina moet bestaan, want na aanmelden wordt hierheen doorgestuurd en de middleware
beschermt dit pad al. Zonder de pagina eindigt een geslaagde aanmelding op een 404, wat
eruitziet alsof het inloggen zelf niet werkt. Fase 2 vervangt de inhoud.

- [ ] **Stap 3: Schrijf het gebruikersscherm**

Een tabel met naam, e-mailadres, rol, toestand en laatste aanmelding, met per regel de
mogelijkheid te deactiveren of opnieuw uit te nodigen. Daarboven een formulier om iemand toe
te voegen.

Toon per gebruiker of er een wachtwoord is ingesteld. Zonder wachtwoord en zonder Entra
betekent dat iemand nog nooit is binnengekomen — dat is precies wat een beheerder wil zien
als iemand meldt dat het niet lukt.

- [ ] **Stap 4: Controleer met de hand**

- Een `VIEWER` die `/beheer/gebruikers` opvraagt wordt geweigerd.
- Iemand uitnodigen levert een Ethereal-preview op met een werkende link.
- Deactiveren blokkeert het aanmelden daadwerkelijk.

- [ ] **Stap 5: Commit**

```bash
git add src/app/\(protected\) src/features/auth/actions.ts
git commit -m "feat: add user administration screen"
```

---

## Taak 12: Afronden

**Files:**
- Modify: `README.md`, `docs/voortgang.md`

- [ ] **Stap 1: Draai alles**

```bash
npm test
npx tsc --noEmit
npm run build
```

Verwacht: alle tests slagen, geen typefouten, de build komt door.

- [ ] **Stap 2: Vul het README aan**

Een paragraaf over toegang: hoe je de eerste beheerder aanmaakt, hoe uitnodigen werkt, dat
Ethereal wordt gebruikt zolang er geen SMTP is ingesteld, en welke omgevingsvariabelen nodig
zijn om Entra aan te zetten.

- [ ] **Stap 3: Werk `docs/voortgang.md` bij**

Wat er af is, wat er anders liep dan dit plan zei, en wat er nog openstaat.

- [ ] **Stap 4: Commit**

```bash
git add README.md docs/voortgang.md
git commit -m "docs: document access and user management"
```

---

## Wat hierna komt

Fase 2: het zoekscherm. Eigen plan, gebaseerd op dezelfde spec. Dat begint met de presets en
de queries — beide pure functies met de gevulde database eronder om tegen te toetsen.
